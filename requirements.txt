# GTM UAE Partner Pipeline — Python Dependencies
# Install: pip install -r requirements.txt

# ── Core agentic framework ──────────────────────────────────────────────────
langgraph>=0.2.0
langchain-core>=0.2.0

# ── PostgreSQL (async driver + connection pooling) ──────────────────────────
asyncpg>=0.29.0
psycopg2-binary>=2.9.9

# ── Environment variable management ────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data processing ─────────────────────────────────────────────────────────
pandas>=2.0.0
openpyxl>=3.1.0

# ── HTTP client (for enrichment API calls + Groq streaming) ─────────────────
httpx>=0.27.0

# ── Search (for LinkedIn URL discovery) ─────────────────────────────────────
tavily-python>=0.3.0

# ── Telephony & Tunneling (Twilio Voice Agent) ──────────────────────────────
twilio>=9.0.0
pyngrok>=7.0.0
websockets>=12.0

# ── FastAPI (voice agent webhook + WebSocket routes) ─────────────────────────
fastapi>=0.111.0
uvicorn>=0.30.0

# ── Bidirectional voice agent — TTS pipeline ────────────────────────────────
edge-tts>=7.2.8      # Free neural TTS (Microsoft Edge voices, no API key)
miniaudio>=1.71      # MP3→PCM decode + resample, pure C, no ffmpeg needed